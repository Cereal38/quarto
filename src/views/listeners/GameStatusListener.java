package src.views.listeners;

/**
 * The GameStatusListener interface provides a contract for classes that need to
 * be notified when the game status is updated.
 */
public interface GameStatusListener {
  /**
   * This method is called when the game status is updated.
   */
  void update();
}
